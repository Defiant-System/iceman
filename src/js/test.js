
let Test = {
	init() {
		// setTimeout(() => iceman.dispatch({ type: "start-level", arg: 1 }), 1000);
		// setTimeout(() => iceman.board.find(".box.bF").addClass("exit-open"), 500);
	}
};

// DEV-ONLY-START
Test.init();
// DEV-ONLY-END
